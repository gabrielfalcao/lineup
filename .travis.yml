language: python

python:
  - "2.7"

services:
  - redis-server

env:
  - TEST_TYPE=unit
  - TEST_TYPE=functional

cache:
  apt: true
  directories:
    - $HOME/.curds

install:
  - pip install curdling
  - curd install -r requirements.txt

script:
  - make $TEST_TYPE
